do {
	tarai = { x, y, z =>
		if x <= y then
			return y;
		else
			return tarai(tarai(x - 1, y, z), tarai(y - 1, z, x), tarai(z - 1, x, y));
		end
	};
	console.puts("{0} = {1}", "tarai(9, 4, 1)", tarai(9, 4, 1));
};
